import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['https://svc-ffad8d35-morphvm-qng7kqhv.http.cloud.morph.so/']
};

export default nextConfig;
